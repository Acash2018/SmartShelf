{
    "name": "smartshelf-frontend",
    "version": "1.0.0",
    "private": true,
    "dependencies": {
      "axios": "^1.4.0",                  // For API requests
      "react": "^18.2.0",                 // React library
      "react-dom": "^18.2.0",             // React DOM rendering
      "react-scripts": "5.0.1",           // React build and dev scripts
      "web-vitals": "^2.1.4"              // For measuring performance
    },
    "scripts": {
      "start": "react-scripts start",     // Start development server
      "build": "react-scripts build",     // Build the app for production
      "test": "react-scripts test",       // Run tests
      "eject": "react-scripts eject"      // Eject config (use cautiously)
    },
    "eslintConfig": {
      "extends": [
        "react-app",
        "react-app/jest"
      ]
    },
    "browserslist": {
      "production": [
        ">0.2%",
        "not dead",
        "not op_mini all"
      ],
      "development": [
        "last 1 chrome version",
        "last 1 firefox version",
        "last 1 safari version"
      ]
    },
    "devDependencies": {
      "eslint": "^8.48.0"                // Optional for linting
    }
  }
  